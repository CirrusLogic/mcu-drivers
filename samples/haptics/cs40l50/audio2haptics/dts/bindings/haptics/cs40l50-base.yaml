description: CS40L50 Haptic Driver

compatible: "cirrus,cs40l50_alt_os"

include: ["i2c-device.yaml"]

properties:
  reset-gpios:
    type: phandle-array
    required: true
    description: Host GPIO pin mapped to CS40L50 reset pin.